# Items

One JSON array per category. Each element is a single item record with
the fields defined in `26-phase-21-items.txt`. Top-level keys: `id`,
`display_name`, `weight`, `value`, `category`, `tags`, `effects`.
